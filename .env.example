# =============================================================================
# ppxai Configuration - Secrets Only
# =============================================================================
# This file contains ONLY sensitive API keys and secrets.
# Provider configuration is in ppxai-config.json (see ppxai-config.example.json)
#
# IMPORTANT: Never commit this file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Configuration File (Optional)
# -----------------------------------------------------------------------------
# Override the default config file search path:
#   1. ./ppxai-config.json (project-specific)
#   2. ~/.ppxai/ppxai-config.json (user-specific)
#   3. Built-in defaults (Perplexity only)
#
# Uncomment to specify a custom config file location:
# PPXAI_CONFIG_FILE=/path/to/your/ppxai-config.json

# -----------------------------------------------------------------------------
# Provider Selection (Optional)
# -----------------------------------------------------------------------------
# Override default_provider from ppxai-config.json
# MODEL_PROVIDER=perplexity

# -----------------------------------------------------------------------------
# API Keys
# -----------------------------------------------------------------------------
# These are referenced by api_key_env in ppxai-config.json

# Perplexity AI
PERPLEXITY_API_KEY=pplx-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# OpenAI ChatGPT (if using openai provider)
# OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# OpenRouter (for Claude, Gemini, Llama, etc.)
# OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Local models (vLLM, Ollama) - can be any value or "dummy"
# LOCAL_API_KEY=dummy-key
# OLLAMA_API_KEY=ollama

# Custom provider API key (for backward compatibility)
# CUSTOM_API_KEY=your-api-key-here

# -----------------------------------------------------------------------------
# SSL Configuration (Optional)
# -----------------------------------------------------------------------------
# Set to false for corporate proxies with self-signed certificates
# Applies to all HTTPS connections
# SSL_VERIFY=true

# -----------------------------------------------------------------------------
# Backward Compatibility (Deprecated)
# -----------------------------------------------------------------------------
# The following CUSTOM_* variables are still supported for backward
# compatibility but are deprecated. Please migrate to ppxai-config.json.
#
# CUSTOM_MODEL_ENDPOINT=http://localhost:8000/v1
# CUSTOM_PROVIDER_NAME=My Self-Hosted LLM
# CUSTOM_MODEL_ID=my-model
# CUSTOM_MODEL_NAME=My Model
# CUSTOM_MODEL_DESC=Description of my model
